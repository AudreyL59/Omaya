<?xml version="1.0" encoding="UTF-8"?>
<!--
  web.config du SOUS-DOSSIER /vendeur.
  Destination : D:\Sites\groupeOmaya\www\vendeur\web.config
  Role : SPA fallback pour React Router.
  Pre-requis : module IIS "URL Rewrite" installe.

  Note : la regle de reverse proxy /api -> uvicorn + les en-tetes de securite
  sont dans le web.config de la RACINE (voir deploy/web.config.root.snippet).
-->
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <!-- SPA fallback : toute URL sous /vendeur/ qui ne correspond pas a un fichier reel -> index.html -->
        <rule name="SPAFallback" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
          </conditions>
          <action type="Rewrite" url="/vendeur/index.html" />
        </rule>
      </rules>
    </rewrite>
  </system.webServer>
</configuration>
